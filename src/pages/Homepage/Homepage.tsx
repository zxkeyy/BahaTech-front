import { Box } from "@chakra-ui/react";
import Hero from "./Components/Hero";

const HomePage = () => {
  return (
    <Box bgColor={"bg.500"}>
      <Hero />
    </Box>
  );
};

export default HomePage;
